{
  "name": "bandctl-webui-tests",
  "version": "1.0.0",
  "private": true,
  "description": "DEV-ONLY jsdom regression harness for web/index.html (behavior + a11y suites). Not part of the shipped module (the module ships bundled Python; no Node runtime).",
  "type": "commonjs",
  "scripts": {
    "test": "node tests/js/run.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
